name: neplox_linkpreviewer
description: Responsive Flutter link previews with metadata fetching, caching, customizable cards, and fixed or content-wrapping layouts.
version: 1.1.0
homepage: https://github.com/starbibek/neplox_linkpreviewer
repository: https://github.com/starbibek/neplox_linkpreviewer
issue_tracker: https://github.com/starbibek/neplox_linkpreviewer/issues

topics:
  - flutter
  - link-preview
  - metadata
  - open-graph
  - url-preview

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.4
  http: ^1.1.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
