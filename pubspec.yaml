name: neplox_linkpreviewer
description: LinkPreviewer Package to show Normal URL link elements and it also provide Element object for custom UI implementation.
version: 1.0.8
homepage: https://github.com/starbibek/neplox_linkpreviewer

environment:
  sdk: '>=2.18.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.4
  http: ^1.1.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
